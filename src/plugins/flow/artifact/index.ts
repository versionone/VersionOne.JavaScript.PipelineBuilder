import { load_revision } from './load_revision';
import { new_revision } from './new_revision';
import { update_revision } from './update_revision';

export const artifact = {
    load_revision,
    new_revision,
    update_revision
};